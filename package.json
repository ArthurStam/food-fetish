{
  "dependencies": {
    "@vkontakte-internal/vkui-common": "^8.0.0-alpha.84",
    "@vkontakte/icons": "^1.91.0",
    "@vkontakte/vk-bridge": "^2.4.1",
    "@vkontakte/vkjs": "^0.21.0",
    "@vkontakte/vkui": "^4.5.0",
    "react": "^16.14.0",
    "react-dom": "^16.14.0"
  },
  "scripts": {
    "deploy": "yarn build && yarn gh-pages",
    "gh-pages": "gh-pages -d build -b gh-pages",
    "start": "npx vkui-app-start --ts --eslint --stylelint",
    "build": "npx vkui-app-build --ts --eslint --stylelint"
  },
  "devDependencies": {
    "gh-pages": "^3.1.0"
  }
}
